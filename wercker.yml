box: debian
build-guediao:
    steps:
        - arjen/hugo-build: 
            config: config.guediao.toml
build-push:
    docker: true
    steps:
        - jakubriedl/docker-build
        - jakubriedl/docker-push:
            image: visnz/blog:latest
            email: $EMAIL
            username: $USERNAME
            password: $PASSWORD 
build-guediao:
    box: debian
    steps:
        - arjen/hugo-build: 
            config: config.guediao.toml
build-push:
    box: alpine
    docker: true
    steps:
        - jakubriedl/docker-build
        - internal/docker-push:
            username: $USERNAME
            password: $PASSWORD
            repository: visnz/blog